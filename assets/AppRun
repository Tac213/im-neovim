#!/bin/bash
# AppImage entry point for ImNeovim.
# Resolves the AppDir and launches imnvim with the bundled Neovim on PATH.

HERE="$(dirname "$(readlink -f "$0")")"
export PATH="$HERE/nvim/bin:$HERE:$PATH"
exec "$HERE/imnvim" "$@"
